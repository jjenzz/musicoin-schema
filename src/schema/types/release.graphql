type Release implements Node {
	id: String
	title: String
	audioUrl: String
}

type ReleaseList implements List {
	count: Int!
	totalPlays: Int!
	nodes: [Release]!
}

type Query {
	allReleases: ReleaseList!
}
